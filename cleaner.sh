[ -d build ] && rm -rf build
[ -d executable ] && rm -rf executable
[ -e image_out.txt ] && rm image_out.txt
[ -e label_out.txt ] && rm label_out.txt
[ -e log_predictions-ci.txt ] && rm log_predictions-ci.txt
[ -e log_predictions.txt ] && rm log_predictions.txt